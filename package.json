{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,

  "scripts": {
    "build": "pnpm run typecheck && pnpm run build:all",
    "build:all": "pnpm --filter @workspace/grado run build && pnpm --filter @workspace/api-server run build",
    "build:railway": "pnpm --filter @workspace/grado run build && pnpm --filter @workspace/api-server run build",
    "start": "pnpm --filter @workspace/api-server run start",
    "typecheck": "tsc -b"
  },

  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },

  "dependencies": {
    "qrcode.react": "^4.2.0"
  },

  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
