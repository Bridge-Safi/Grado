[build]
builder = "NIXPACKS"
buildCommand = "npm install -g pnpm@10.15.0 && npx pnpm@10.15.0 install --no-frozen-lockfile && npx pnpm@10.15.0 run build:railway"

[deploy]
startCommand = "NODE_ENV=production npx pnpm@10.15.0 --filter @workspace/api-server run start"
