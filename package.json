{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,

  "scripts": {
    "build": "npm run typecheck && npm run build:all",
    "build:all": "npm run typecheck && npm run build --workspaces",
    "typecheck": "tsc -b"
  },

  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },

  "dependencies": {
    "qrcode.react": "^4.2.0"
  }
}
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },

  "dependencies": {
    "qrcode.react": "^4.2.0"
  }
}
