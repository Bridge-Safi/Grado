:{$PORT} {
    handle /api/* {
        reverse_proxy grado-workspaceapi-server-production.up.railway.app {
            header_up Host {upstream_hostport}
        }
    }
    handle {
        root * /app/artifacts/grado/dist/public
        file_server
        try_files {path} /index.html
    }
}
